services:
  auth-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: auth-service
    ports:
      - "8888:8888"
    restart: unless-stopped
    networks:
      - auth-network

networks:
  auth-network:
    name: auth-network
    external: true
